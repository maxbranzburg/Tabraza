 <div class="container">

<!--         <header class="jumbotron hero-spacer">
	        <h2>Welcome to Tabraza!</h2>
	        <p><p><em>Take your dream courses from the world's top universities</em></p></p>
        </header> -->
        
        <div class="center-block">

	        <div class="row text-center">
	           <h3>Browse Popular Concentrations</h3>
	        </div>

            <section class="curriculum" id="a">
                <div class="row text-left">
                    <div class="curriculum_title">
                        <h3><%= @concentration_a.name %></h3>
                    </div>
                </div>
                <div class="row text-center">
                <% @concentration_a.courses.each do |course| %>
        				<div class="col-md-3 col-sm-6 hero-feature">
                            <%= link_to image_tag(course.photo, class:"course_photo"), course %>
                            <div class="caption">
                                <h6><%= course.name %></h6>
                            </div>
        	            </div>
                <% end %>
                </div>
            </section>

            <section class="curriculum" id="b">
                <div class="row text-left">
                    <div class="curriculum_title">
                        <h3><%= @concentration_b.name %></h3>
                    </div>
                </div>
                <div class="row text-center">
                <% @concentration_b.courses.each do |course| %>
                        <div class="col-md-3 col-sm-6 hero-feature">
                            <%= link_to image_tag(course.photo, class:"course_photo"), course %>
                            <div class="caption">
                                <h6><%= course.name %></h6>
                            </div>
                        </div>
                <% end %>
                </div>
            </section>

            <section class="curriculum" id="c">
                <div class="row text-left">
                    <div class="curriculum_title">
                        <h3><%= @concentration_c.name %></h3>
                    </div>
                </div>
                <div class="row text-center">
                <% @concentration_c.courses.each do |course| %>
                        <div class="col-md-3 col-sm-6 hero-feature">
                            <%= link_to image_tag(course.photo, class:"course_photo"), course %>
                            <div class="caption">
                                <h6><%= course.name %></h6>
                            </div>
                        </div>
                <% end %>
                </div>
            </section>

            <div class="row text-center">
               <h4><%= link_to "More Concentrations", concentration_index_path %></h4>
            </div>

		</div>

    	<!-- /.container -->